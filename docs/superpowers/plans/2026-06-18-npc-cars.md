# NPC Araba Sistemi — İmplementasyon Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sahneye NavMesh + WheelCollider tabanlı 3-5 NPC trafik arabası eklemek; araçlar navmesh üzerinde dolaşır, önlerindeki engeli raycast ile algılar ve oyuncuya yakınlaşınca yavaşlar.

**Architecture:** NavMeshAgent path hesaplamayı üstlenir (`updatePosition=false`, `updateRotation=false`). `NpcCarController` her FixedUpdate'te `agent.steeringTarget`'ı okur, local space'e çevirir, WheelCollider steer + motor torque'u uygular ve `agent.nextPosition = transform.position` ile agentı senkronize tutar. `NpcTrafficManager` sahne başında NPC prefab'larını spawn eder; hedefe ulaşan her araca yeni rastgele NavMesh noktası atar.

**Tech Stack:** Unity 6, C#, Unity AI Navigation (`com.unity.ai.navigation`), WheelCollider, Rigidbody, NavMeshAgent, Physics.Raycast

> **Not — TDD adaptasyonu:** Unity MonoBehaviour + physics için pre-compile unit test yazmak impractical. Her task'ın sonunda PlayMode'da manuel doğrulama adımları verilmiştir; bunlar "testi çalıştır ve geç/başarısız" adımlarının karşılığıdır.

---

## Dosya Haritası

| Durum | Yol | Sorumluluk |
|---|---|---|
| Oluştur | `Assets/Scripts/NpcCarController.cs` | NavMesh → WheelCollider köprüsü, orta zeka |
| Oluştur | `Assets/Scripts/NpcTrafficManager.cs` | Spawn, hedef atama, döngüsel trafik |
| Oluştur | `Assets/Prefabs/NpcCar.prefab` | WheelCollider + NavMeshAgent hazır prefab (Editor'de) |
| Değiştir | `Assets/Scenes/SampleScene.unity` | NavMeshSurface + TrafficManager GameObject eklenir (Editor'de) |
| Dokunma | `Assets/Scripts/CarController.cs` | Oyuncu arabası — değişmez |

---

## Task 1: Unity AI Navigation Paketini Yükle

**Files:**
- Modify: `Packages/manifest.json` (Package Manager otomatik günceller)

- [ ] **Step 1: Paketi yükle**

  Unity'de: **Window → Package Manager → + → Add package by name**
  
  Paket adı:
  ```
  com.unity.ai.navigation
  ```
  
  Install'a tıkla. Unity indirip derleyene kadar bekle.

- [ ] **Step 2: Yüklemeyi doğrula**

  Herhangi bir GameObject seç → **Add Component** → "NavMeshSurface" yazı çıkıyorsa yükleme başarılı.
  
  Çıkmıyorsa: Package Manager'da `com.unity.ai.navigation` listede görünüyor mu kontrol et.

- [ ] **Step 3: Commit**

  ```bash
  git add Packages/
  git commit -m "feat: add Unity AI Navigation package"
  ```

---

## Task 2: Oyuncu Arabasını Etiketle

**Files:**
- Modify: `Assets/Scenes/SampleScene.unity` (Unity Editor kaydeder)

`NpcCarController`, oyuncu arabasını `FindWithTag("Player")` ile bulur. Tag ayarlanmazsa NPC oyuncuyu algılamaz.

- [ ] **Step 1: Player tag'ini ata**

  Hierarchy'de oyuncu arabasının root GameObject'ini seç.
  
  Inspector'da en üstteki **Tag** dropdown → **Player** seç.
  
  Eğer "Player" tag yoksa: **Add Tag** → + → "Player" yaz → Save.

- [ ] **Step 2: Sahneyi kaydet ve commit et**

  **Ctrl+S** ile sahneyi kaydet.
  
  ```bash
  git add Assets/Scenes/SampleScene.unity
  git commit -m "feat: tag player car as Player"
  ```

---

## Task 3: NpcCarController.cs Yaz

**Files:**
- Create: `Assets/Scripts/NpcCarController.cs`

- [ ] **Step 1: Dosyayı oluştur**

  `Assets/Scripts/NpcCarController.cs` olarak yeni dosya oluştur, aşağıdaki kodu yaz:

  ```csharp
  using UnityEngine;
  using UnityEngine.AI;

  public class NpcCarController : MonoBehaviour
  {
      [Header("Wheel Colliders")]
      public WheelCollider frontLeftWheelCollider;
      public WheelCollider frontRightWheelCollider;
      public WheelCollider rearLeftWheelCollider;
      public WheelCollider rearRightWheelCollider;

      [Header("Wheel Transforms (Visuals)")]
      public Transform frontLeftWheelTransform;
      public Transform frontRightWheelTransform;
      public Transform rearLeftWheelTransform;
      public Transform rearRightWheelTransform;

      [Header("Car Settings")]
      public float motorForce = 800f;
      public float brakeForce = 2000f;
      public float maxSteeringAngle = 30f;
      public float targetSpeed = 8f;

      [Header("AI Settings")]
      public float detectionRange = 15f;
      public float stopDistance = 3f;
      public float playerDetectRange = 10f;

      private NavMeshAgent agent;
      private Rigidbody rb;
      private Transform playerCar;
      private bool forceStopped;

      private void Awake()
      {
          agent = GetComponent<NavMeshAgent>();
          rb = GetComponent<Rigidbody>();
          agent.updatePosition = false;
          agent.updateRotation = false;
          agent.speed = targetSpeed;
      }

      private void Start()
      {
          GameObject player = GameObject.FindWithTag("Player");
          if (player != null)
              playerCar = player.transform;
      }

      private void FixedUpdate()
      {
          if (forceStopped)
          {
              ApplyBrake(brakeForce);
              return;
          }

          agent.nextPosition = transform.position;
          HandleSteering();
          HandleMotor();
          UpdateWheelVisuals();
      }

      private void HandleSteering()
      {
          Vector3 localTarget = transform.InverseTransformPoint(agent.steeringTarget);
          float steerAngle = Mathf.Atan2(localTarget.x, localTarget.z) * Mathf.Rad2Deg;
          steerAngle = Mathf.Clamp(steerAngle, -maxSteeringAngle, maxSteeringAngle);

          frontLeftWheelCollider.steerAngle = steerAngle;
          frontRightWheelCollider.steerAngle = steerAngle;
      }

      private void HandleMotor()
      {
          float desiredSpeed = CalculateDesiredSpeed();
          float currentSpeed = rb.linearVelocity.magnitude;
          float speedDiff = desiredSpeed - currentSpeed;

          if (speedDiff > 0.1f)
          {
              rearLeftWheelCollider.motorTorque = motorForce;
              rearRightWheelCollider.motorTorque = motorForce;
              ApplyBrake(0f);
          }
          else
          {
              rearLeftWheelCollider.motorTorque = 0f;
              rearRightWheelCollider.motorTorque = 0f;
              float brakePower = Mathf.Clamp01(-speedDiff / targetSpeed) * brakeForce;
              ApplyBrake(brakePower);
          }
      }

      private float CalculateDesiredSpeed()
      {
          Vector3 rayOrigin = transform.position + Vector3.up * 0.5f;
          if (Physics.Raycast(rayOrigin, transform.forward, out RaycastHit hit, detectionRange))
          {
              if (hit.distance <= stopDistance)
                  return 0f;
              return targetSpeed * (hit.distance / detectionRange);
          }

          if (playerCar != null &&
              Vector3.Distance(transform.position, playerCar.position) < playerDetectRange)
              return targetSpeed * 0.7f;

          return targetSpeed;
      }

      private void ApplyBrake(float force)
      {
          frontLeftWheelCollider.brakeTorque = force;
          frontRightWheelCollider.brakeTorque = force;
          rearLeftWheelCollider.brakeTorque = force;
          rearRightWheelCollider.brakeTorque = force;
      }

      private void UpdateWheelVisuals()
      {
          UpdateSingleWheel(frontLeftWheelCollider, frontLeftWheelTransform);
          UpdateSingleWheel(frontRightWheelCollider, frontRightWheelTransform);
          UpdateSingleWheel(rearLeftWheelCollider, rearLeftWheelTransform);
          UpdateSingleWheel(rearRightWheelCollider, rearRightWheelTransform);
      }

      private void UpdateSingleWheel(WheelCollider col, Transform t)
      {
          col.GetWorldPose(out Vector3 pos, out Quaternion rot);
          t.SetPositionAndRotation(pos, rot);
      }

      public void ForceStop(bool stop)
      {
          forceStopped = stop;
      }
  }
  ```

- [ ] **Step 2: Derlemeyi doğrula**

  Unity Console'da `NpcCarController.cs` ile ilgili hata yoksa derleme başarılı.
  
  Hata varsa: `NavMeshAgent` namespace hatası → Task 1'in tamamlandığını kontrol et (`using UnityEngine.AI` gerektirir).

- [ ] **Step 3: Commit**

  ```bash
  git add Assets/Scripts/NpcCarController.cs
  git commit -m "feat: add NpcCarController with NavMesh-WheelCollider bridge"
  ```

---

## Task 4: NpcTrafficManager.cs Yaz

**Files:**
- Create: `Assets/Scripts/NpcTrafficManager.cs`

- [ ] **Step 1: Dosyayı oluştur**

  `Assets/Scripts/NpcTrafficManager.cs` olarak yeni dosya oluştur:

  ```csharp
  using System.Collections.Generic;
  using UnityEngine;
  using UnityEngine.AI;

  public class NpcTrafficManager : MonoBehaviour
  {
      [Header("NPC Settings")]
      public GameObject npcPrefab;
      public Transform[] spawnPoints;
      public int npcCount = 3;
      public float waypointRadius = 5f;
      public float destinationReachedThreshold = 2f;

      private readonly List<NavMeshAgent> npcAgents = new();

      private void Start()
      {
          SpawnNpcs();
      }

      private void Update()
      {
          foreach (NavMeshAgent agent in npcAgents)
          {
              if (!agent.pathPending && agent.remainingDistance < destinationReachedThreshold)
                  AssignNewDestination(agent);
          }
      }

      private void SpawnNpcs()
      {
          int count = Mathf.Min(npcCount, spawnPoints.Length);
          for (int i = 0; i < count; i++)
          {
              GameObject npc = Instantiate(npcPrefab, spawnPoints[i].position, spawnPoints[i].rotation);
              NavMeshAgent agent = npc.GetComponent<NavMeshAgent>();
              npcAgents.Add(agent);
              AssignNewDestination(agent);
          }
      }

      public void SpawnNpc(Transform spawnPoint)
      {
          GameObject npc = Instantiate(npcPrefab, spawnPoint.position, spawnPoint.rotation);
          NavMeshAgent agent = npc.GetComponent<NavMeshAgent>();
          npcAgents.Add(agent);
          AssignNewDestination(agent);
      }

      private void AssignNewDestination(NavMeshAgent agent)
      {
          agent.SetDestination(SampleRandomNavMeshPoint());
      }

      private Vector3 SampleRandomNavMeshPoint()
      {
          for (int i = 0; i < 10; i++)
          {
              Vector3 candidate = transform.position + Random.insideUnitSphere * 50f;
              candidate.y = 0f;
              if (NavMesh.SamplePosition(candidate, out NavMeshHit hit, waypointRadius, NavMesh.AllAreas))
                  return hit.position;
          }
          return transform.position;
      }
  }
  ```

- [ ] **Step 2: Derlemeyi doğrula**

  Unity Console'da hata yoksa başarılı.

- [ ] **Step 3: Commit**

  ```bash
  git add Assets/Scripts/NpcTrafficManager.cs
  git commit -m "feat: add NpcTrafficManager for NPC spawn and destination cycling"
  ```

---

## Task 5: NpcCar Prefab'ını Oluştur (Unity Editor)

**Files:**
- Create: `Assets/Prefabs/NpcCar.prefab`

Bu task tamamen Unity Editor'de yapılır. Oyuncu arabasının hiyerarşisini referans al.

- [ ] **Step 1: Klasörü oluştur**

  Project penceresinde `Assets` → sağ tık → **Create → Folder** → `Prefabs` adını ver.

- [ ] **Step 2: NpcCar GameObject oluştur**

  Hierarchy'de sağ tık → **Create Empty** → adını `NpcCar` yap.

- [ ] **Step 3: Rigidbody ekle ve ayarla**

  `NpcCar` seçili, Inspector → **Add Component → Rigidbody**
  
  Ayarlar:
  - Mass: `1000`
  - Drag: `0.05`
  - Angular Drag: `0.05`
  - Use Gravity: ✓
  - Constraints → Freeze Rotation: **X** ve **Z** işaretle (X ✓, Z ✓)

- [ ] **Step 4: NavMeshAgent ekle ve ayarla**

  Inspector → **Add Component → NavMesh Agent**
  
  Ayarlar:
  - Speed: `8`
  - Angular Speed: `120`
  - Acceleration: `8`
  - Stopping Distance: `1`
  - Auto Braking: **Kapat** (unchecked)
  - Radius: `0.8`
  - Height: `1.5`

- [ ] **Step 5: NpcCarController ekle**

  Inspector → **Add Component → Npc Car Controller**

- [ ] **Step 6: Araba modelini ekle**

  Hierarchy'de `NpcCar` altına bir çocuk GameObject oluştur: `CarModel`
  
  `Assets/Models/Tofaş (1).fbx`'i `CarModel`'in altına sürükle.
  
  > İpucu: Aynı modeli kullansanız da renk/materyal farklılaştırmak için `Assets/Models/Materials/` altından farklı bir materyal atayabilirsin.

- [ ] **Step 7: WheelCollider'ları oluştur**

  `NpcCar` altına boş bir çocuk oluştur: `WheelColliders`
  
  `WheelColliders` altında 4 adet boş GameObject oluştur:
  - `FrontLeftWheelCollider`
  - `FrontRightWheelCollider`
  - `RearLeftWheelCollider`
  - `RearRightWheelCollider`
  
  Her birine **Add Component → Wheel Collider** ekle.
  
  Pozisyonlarını oyuncu arabasındaki WheelCollider pozisyonlarıyla aynı yap (Scene view'da bakarak ayarla).
  
  Her WheelCollider için:
  - Mass: `20`
  - Radius: `0.35` (modele göre ayarla)
  - Suspension Distance: `0.2`

- [ ] **Step 8: Görsel tekerlek transform'larını oluştur**

  `NpcCar` altına boş bir çocuk oluştur: `WheelTransforms`
  
  `WheelTransforms` altında 4 boş GameObject:
  - `FrontLeftWheel`
  - `FrontRightWheel`
  - `RearLeftWheel`
  - `RearRightWheel`
  
  Pozisyonları WheelCollider pozisyonlarıyla eşleştir.

- [ ] **Step 9: NpcCarController referanslarını bağla**

  `NpcCar` seçili → Inspector'da `NpcCarController` bileşenine git.
  
  Tüm `WheelCollider` ve `Transform` slotlarını ilgili GameObject'lerle doldur:
  - Front Left Wheel Collider → `FrontLeftWheelCollider`
  - Front Right Wheel Collider → `FrontRightWheelCollider`
  - Rear Left Wheel Collider → `RearLeftWheelCollider`
  - Rear Right Wheel Collider → `RearRightWheelCollider`
  - Front Left Wheel Transform → `FrontLeftWheel`
  - Front Right Wheel Transform → `FrontRightWheel`
  - Rear Left Wheel Transform → `RearLeftWheel`
  - Rear Right Wheel Transform → `RearRightWheel`

- [ ] **Step 10: Prefab olarak kaydet**

  Hierarchy'de `NpcCar`'ı `Assets/Prefabs/` klasörüne **sürükle-bırak**.
  
  Orijinal Hierarchy'deki `NpcCar`'ı silebilirsin (prefab kaydedildi).

- [ ] **Step 11: Commit**

  ```bash
  git add Assets/Prefabs/
  git commit -m "feat: create NpcCar prefab with WheelColliders and NavMeshAgent"
  ```

---

## Task 6: NavMesh Bake (Unity Editor)

**Files:**
- Modify: `Assets/Scenes/SampleScene.unity`

- [ ] **Step 1: NavMeshSurface ekle**

  Hierarchy'de sahnenin zemin düzlemini (ground plane) seç.
  
  Inspector → **Add Component → NavMesh Surface**

- [ ] **Step 2: Agent Type ayarla**

  NavMeshSurface Inspector'da:
  - Agent Type: **Humanoid** (varsayılan yeterli)
  - Collect Objects: **All Game Objects**
  - Include Layers: **Everything**

- [ ] **Step 3: NavMesh'i bake et**

  NavMeshSurface Inspector'da alt taraftaki **Bake** butonuna tıkla.
  
  Scene view'da zemin mavi renk aldıysa bake başarılı.
  
  Mavi yoksa: zemin GameObject'inin `Navigation Static` olduğundan emin ol (Inspector'da Static checkbox → Navigation Static ✓).

- [ ] **Step 4: Sahneyi kaydet ve commit et**

  **Ctrl+S**
  
  ```bash
  git add Assets/Scenes/SampleScene.unity
  git commit -m "feat: bake NavMesh on scene ground plane"
  ```

---

## Task 7: TrafficManager'ı Sahneye Ekle ve Test Et

**Files:**
- Modify: `Assets/Scenes/SampleScene.unity`

- [ ] **Step 1: TrafficManager GameObject oluştur**

  Hierarchy → sağ tık → **Create Empty** → adını `TrafficManager` yap.
  
  Pozisyon: `(0, 0, 0)`

- [ ] **Step 2: NpcTrafficManager bileşenini ekle**

  `TrafficManager` seçili → **Add Component → Npc Traffic Manager**

- [ ] **Step 3: SpawnPoint'leri oluştur**

  Hierarchy → `TrafficManager` altına 3 adet boş çocuk oluştur:
  - `SpawnPoint1` — Pozisyon: `(10, 0, 10)`
  - `SpawnPoint2` — Pozisyon: `(-10, 0, 10)`
  - `SpawnPoint3` — Pozisyon: `(0, 0, -10)`
  
  Her SpawnPoint NavMesh üzerinde bir nokta olmalı (Scene view'da mavi alan içinde).

- [ ] **Step 4: NpcTrafficManager Inspector referanslarını bağla**

  `TrafficManager` seçili → Inspector'da:
  - **Npc Prefab** → `Assets/Prefabs/NpcCar.prefab` sürükle
  - **Spawn Points** → Size: `3`, ardından SpawnPoint1/2/3'ü sırayla sürükle
  - **Npc Count**: `3`
  - **Waypoint Radius**: `5`
  - **Destination Reached Threshold**: `2`

- [ ] **Step 5: PlayMode testi — NPC hareket**

  **Play** butonuna bas.
  
  Beklenen: 3 NPC prefabı SpawnPoint'lerde spawn olur ve yavaşça ileri hareket eder.
  
  Sorun — NPC hareketsiz kalıyorsa:
  - Console'da path hatası var mı? SpawnPoint'ler NavMesh üzerinde mi?
  - NavMeshAgent'ın `isStopped` false mı? (Inspector'da Play sırasında kontrol et)

- [ ] **Step 6: PlayMode testi — Raycast engel algılama**

  Play sırasında oyuncu arabasını bir NPC'nin önüne sürgü.
  
  Beklenen: NPC yavaşlar ve durur (stopDistance = 3m içine girince).
  
  Scene view'da **Gizmos** açık ve Debug.DrawRay ile görselleştirmek istersen `NpcCarController.CalculateDesiredSpeed()`'e ekleyebilirsin:
  ```csharp
  Debug.DrawRay(rayOrigin, transform.forward * detectionRange, Color.red);
  ```

- [ ] **Step 7: PlayMode testi — Oyuncu farkındalığı**

  Oyuncu arabasını bir NPC'ye 10m'den yaklaştır.
  
  Beklenen: NPC hızı yaklaşık %70'e düşer (normal hız 8 m/s → ~5.6 m/s).
  
  Doğrulama: `NpcCarController`'da `CalculateDesiredSpeed()`'e geçici `Debug.Log($"Speed: {desiredSpeed}");` ekleyip Console'dan kontrol edebilirsin.

- [ ] **Step 8: Sahneyi kaydet ve commit et**

  **Ctrl+S**
  
  ```bash
  git add Assets/Scenes/SampleScene.unity
  git commit -m "feat: configure TrafficManager with spawn points and NPC prefab"
  ```

---

## Tamamlandı

**Çalışan sistem:**
- 3 NPC araç NavMesh üzerinde rastgele dolaşır
- Hedefine ulaşan araç yeni hedef alır (döngüsel trafik)
- Önünde araç/nesne varsa yavaşlar ve durur
- Oyuncuya 10m yaklaşınca defansif yavaşlama uygular
- `ForceStop(bool)` metodu trafik ışığı entegrasyonu için hazır

**İleride eklenebilir (bu sprint dışı):**
- Trafik ışığı sistemi → `npcController.ForceStop(true/false)` çağrısı
- Daha fazla NPC → Inspector'da `npcCount` artır, SpawnPoint ekle, Rebake
- Şehir/yol modeli → Sahneye ekle, NavMeshSurface Rebake
